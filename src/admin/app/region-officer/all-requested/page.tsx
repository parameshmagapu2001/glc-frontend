// sections
import TotalRequestInfoListView from "src/sections/region-officer/total-request-info-list-view";

// ----------------------------------------------------------------------

export const metadata = {
  title: 'GLC : Region Officer',
};

export default function RegionOfficerPage() {
  return (
      <TotalRequestInfoListView />
  );
}
