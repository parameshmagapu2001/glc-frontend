// sections
import SessionExpiredView from 'src/sections/auth/session-expired-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'AK Session Expired',
};

export default function SessionPage() {
  return <SessionExpiredView/>;
}
