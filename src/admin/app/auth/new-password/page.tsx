// sections
import NewPasswordView from 'src/sections/auth/new-password-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'AK New Password',
};

export default function NewPasswordPage() {
  return <NewPasswordView />;
}
