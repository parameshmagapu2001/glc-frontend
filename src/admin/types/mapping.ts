export type IMapping = {
    id: string;
    label: string;
};

export type ICodeMapping = {
    id: string;
    label: string;
    code: string;
};

export type IMappingTableFilterValue = string | string[];

export type IMappingTableFilters = {
    label: string;
};